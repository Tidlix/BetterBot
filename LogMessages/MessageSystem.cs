namespace BetterBot.LogMessages {
    public class MessageSystem {
        public static void sendMessage (string message, Task MessageType) {
            Console.WriteLine(message);
        }
    }
}